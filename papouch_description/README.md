# papouch_description

This package contains a URDF description of the Papouch-controlled gripper.

The actuated gripper joint is named `joint_gripper`, matching the default
configuration used by `papouch_ros` for the Schunk gripper node.

## View the model

``` shell
ros2 launch papouch_description display.launch.py
```
